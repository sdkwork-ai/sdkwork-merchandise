-- ============================================================================
-- zh-CN locale seed: intentionally writes no translation rows.
-- ============================================================================
-- zh-CN is this module's default locale (seeds/seed.manifest.json
-- `defaultLocale` and database.manifest.json `defaultSeedLocale`), so the zh-CN
-- display values are carried by the base tables in
-- seeds/common/001_merchandise_bootstrap.sql and serve as the fallback for
-- every locale that has no translation row.
--
-- Writing zh-CN rows into the translation tables would duplicate the base
-- values and let the two copies diverge, so this file deliberately does not do
-- it. It exists because the seed manifest contract requires a file per active
-- locale.
--
-- The previous version of this seed wrote `name` and `spec_json` back into
-- `commerce_product_sku`, which meant zh-CN and en-US overwrote each other in a
-- single base column. That is fixed by the *_translation tables; see
-- seeds/locales/en-US/001_sku_locale.sql for the override pattern.
-- ============================================================================

-- No-op by design.
SELECT 1;
